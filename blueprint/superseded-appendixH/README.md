# Superseded: the Appendix-H-derived draft

`exposition/` here holds 33 proof sketches condensed from `app_doubling.tex`'s own
`\begin{proof}` blocks, and `blueprint.py` is the generator that assembled them into a draft.

They are kept only because nothing in this tree is committed yet. They are **not** used: their
provenance is the appendix's prose proofs, which are not machine-checked, and the appendix is
now generated from the Lean development instead. See `blueprint/README.md`.
